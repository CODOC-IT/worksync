import React from 'react';
import { ProjectChatsView } from '../project-chats/ProjectChatsView';

/**
 * ProjectChatView - Azeem Phase 2
 * Wrapper that renders the ProjectChatsView for the 'chat' route alias.
 * The full implementation lives in project-chats/ProjectChatsView.tsx.
 */
export const ProjectChatView: React.FC = () => {
  return <ProjectChatsView />;
};
